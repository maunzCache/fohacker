'use strict';

class Dud {
    constructor(dudPos, startDud, endDud) {
        // TODO: Rename to index or someting than makes more sense
        this.dudPos = dudPos
        this.startDud = startDud,
        this.endDud = endDud;
    }
}

module.exports = Dud;
