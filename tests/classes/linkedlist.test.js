'use strict';

import LinkedList from '../../src/classes/linkedlist.js';

describe('LinkedList', () => {
    test('constructor', () => {
        let actualResult = new LinkedList();

        expect(actualResult);
    });
});
